== CURRENT STATUS ==

Rendering engine:
  [x] CSS-like cascade, selectors, specificity
  [x] Block/flex layout with finalize_positions
  [x] femtovg paint pass (background, border, text)
  [x] Swash hinting glyph cache (manual Rgba8 atlas)
  [x] OpenSans (sans) + FiraMono (mono) fonts embedded
  [x] femtovg swash feature enabled (used in F-key debug path)
  [x] Performance tests for layout/style (perf_layout_deep_tree, perf_layout_flat_flex, perf_style_cascade)
  [x] CSS-like declarative text language for stylesheets (css_parse.rs; assets/main.css)
  [x] HTML-like declarative text language for UI layout (html_parse.rs; parse_html() → Node tree)

Hot exit (session):
  [x] SQLite schema + CRUD (WAL, bundled)
  [x] Buffer + undo/redo in-memory model
  [x] Session struct with sync logic
  [x] Wire session into event loop (typing, undo/redo, Ctrl+O/S, periodic sync, autoload on boot)

Editor (basic wired):
  [x] Keyboard input (typing, backspace, delete, enter, arrows, home/end, ctrl+z/y/s/o)
  [x] Cursor tracking (line/col display in statusbar)
  [x] Multi-buffer tabs (tab bar, tab switching by click)
  [x] File open via rfd native dialog (Ctrl+O / File > Open)
  [x] File save (Ctrl+S / File > Save)
  [x] Dropdown menus (File, Edit) with position:absolute overlay
  [ ] Visual cursor rendering (blinking caret in editor)
  [ ] Scrolling (editor content > window height)
  [ ] Line numbers
  [ ] Syntax highlighting

== ORIGINAL PROMPT (before any work began) ==

Let's write a programming text editor!

The two core philosophies:

1) Complete, total, perfect hot exit implementation, including undo/redo history and unsaved buffers. The program can hard-crash or get nuked from orbit mid-operation and still end up with a coherent, recent load state. How? Dumping everything to an sqlite db as it runs. Every 5 minutes sync up the file contents and undo histories for anything that's changed since the last sync. And autoload it without asking the user on boot. Do it efficiently.

2) Custom HTML/CSS-like rendering engine, but *not* actually HTML/CSS. It must have a cascade and selector system like CSS, and a vaguely HTML-like layout model. Write lots of static automatic performance tests.
